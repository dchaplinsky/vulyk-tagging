============
Installation
============

At the command line::

    $ easy_install vulyk-tagging

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv vulyk-tagging
    $ pip install vulyk-tagging
